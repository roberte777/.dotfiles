# Provisioned so Grafana comes up with Loki connected after a volume reset.

apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    uid: loki
    url: http://loki:3100
    isDefault: true
    jsonData:
      maxLines: 5000
    editable: false
