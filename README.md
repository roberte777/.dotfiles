My setup uses awesome window manager (installed with apt)
rofi for file runner, basically what I use the windows key for on windows (installed with apt)
kitty terminal (installed from kitty website)

